/**
 * Agar.io Clone — Local Server
 * ============================
 * Drop-in local server. No build step required.
 *
 * Run:  node server.js            (default port 3000)
 *       PORT=8080 node server.js  (custom port)
 *
 * Then open  http://localhost:3000  in your browser.
 * Share that URL with friends on the same network to play together.
 */

'use strict';

require('./server/server');
